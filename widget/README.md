# FashionFit Widget

Dependency-free Vanilla JS widget embedded into WooCommerce product pages. It
renders the "try it on" button and drives the virtual try-on flow against the
FashionFit backend API.
